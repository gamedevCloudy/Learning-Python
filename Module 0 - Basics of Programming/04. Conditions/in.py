# The "in" operator could be used to check if a 
# specified object exists within an iterable object container, 
# such as a list:

name = "John"
if name in ["John", "Rick"]:
    print("Your name is either John or Rick.")